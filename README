# xheads

xheads is a GO monitor management tool designed to detect the attached
displays and read layouts and configuration settings for each display from
a configuration file and set the correct layout and config on change.

### Run
```
go run main.go
```
